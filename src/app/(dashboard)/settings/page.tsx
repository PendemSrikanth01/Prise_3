import { Role } from '@prisma/client';
import { createPersonAction, updatePersonAccessAction } from '@/app/actions/workflows';
import { SubmitButton } from '@/components/ui/FormButtons';
import { hasPermission, requireSession } from '@/lib/auth';
import { prisma } from '@/lib/prisma';

export const dynamic = 'force-dynamic';

export default async function SettingsPage() {
  const session = await requireSession();
  const canManage = hasPermission(session.user.role, 'people:manage');
  const [people, startups] = canManage ? await Promise.all([
    prisma.person.findMany({ orderBy: { name: 'asc' }, select: { id: true, name: true, email: true, role: true, isActive: true, lastLoginAt: true } }),
    prisma.startup.findMany({ orderBy: { sNo: 'asc' }, select: { id: true, name: true } }),
  ]) : [[], []];
  return <div className="mx-auto w-full max-w-6xl p-4 sm:p-6 lg:p-8"><h1 className="text-2xl font-bold tracking-tight">Administration</h1><p className="mt-2 text-sm text-prise-text-secondary">Identity, access and deployment posture.</p><div className="mt-6 rounded-card border bg-white p-5 shadow-card"><div className="text-sm font-semibold">Your access</div><div className="mt-3 grid gap-3 text-sm sm:grid-cols-3"><Info label="Account" value={session.user.email} /><Info label="Role" value={session.user.role.replaceAll('_', ' ').toLowerCase()} /><Info label="Session expires" value={session.expiresAt.toLocaleString('en-IN')} /></div></div>{canManage ? <><section className="mt-5 rounded-card border bg-white p-5 shadow-card sm:p-6"><h2 className="font-semibold">Create account</h2><p className="mt-1 text-sm text-prise-text-secondary">The person must change the temporary password on first sign-in.</p><form action={createPersonAction} className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3"><input name="name" required placeholder="Full name" className="h-11 rounded-input border px-3" /><input name="email" type="email" required placeholder="Email" className="h-11 rounded-input border px-3" /><input name="phone" placeholder="Phone (optional)" className="h-11 rounded-input border px-3" /><select name="role" defaultValue={Role.MENTOR} className="h-11 rounded-input border bg-white px-3">{Object.values(Role).map((value) => <option key={value}>{value}</option>)}</select><select name="founderOfStartupId" className="h-11 rounded-input border bg-white px-3"><option value="">Founder startup (only if applicable)</option>{startups.map((startup) => <option key={startup.id} value={startup.id}>{startup.name}</option>)}</select><input name="password" type="password" required minLength={12} placeholder="Temporary password · 12+ chars" className="h-11 rounded-input border px-3" /><div className="lg:col-span-3"><SubmitButton>Create secure account</SubmitButton></div></form></section><section className="mt-5 overflow-hidden rounded-card border bg-white shadow-card"><div className="border-b px-5 py-4"><h2 className="font-semibold">Role and account status</h2></div><div className="divide-y">{people.map((person) => <form action={updatePersonAccessAction} key={person.id} className="grid gap-3 px-5 py-4 md:grid-cols-[1.2fr_1fr_160px_100px_auto] md:items-center"><input type="hidden" name="personId" value={person.id} /><div><div className="text-sm font-semibold">{person.name}</div><div className="text-xs text-prise-text-secondary">{person.email}{person.lastLoginAt ? ` · last login ${person.lastLoginAt.toLocaleDateString('en-IN')}` : ' · never signed in'}</div></div><select name="role" defaultValue={person.role} className="h-10 rounded-input border bg-white px-3 text-sm">{Object.values(Role).map((value) => <option key={value}>{value}</option>)}</select><label className="flex items-center gap-2 text-sm"><input name="isActive" type="checkbox" defaultChecked={person.isActive} className="accent-prise-primary" /> Active</label><div className={`text-xs font-semibold ${person.isActive ? 'text-success' : 'text-danger'}`}>{person.isActive ? 'Enabled' : 'Disabled'}</div><SubmitButton className="!py-2">Save access</SubmitButton></form>)}</div></section></> : <div className="mt-5 rounded-card border border-warning/20 bg-warning-bg p-5 text-sm text-prise-text-secondary">Only the program lead can create accounts or change roles.</div>}</div>;
}
function Info({ label, value }: { label: string; value: string }) { return <div className="rounded-input bg-prise-page p-3"><div className="text-xs text-prise-text-muted">{label}</div><div className="mt-1 truncate font-medium capitalize">{value}</div></div>; }
